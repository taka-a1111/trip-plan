---
name: trip-itinerary-planner
description: 行きたい場所・飲食店のリストと日程から、回りやすい旅行プラン（旅程）を組み立てる汎用スキル。行き先は問わない（国内・海外どこでも）。「旅行プランを作って」「行きたい場所リストで日程を組んで」「この旅程を回りやすくして」「しおりを作って」などの依頼で必ず使う。まず簡潔なテキスト計画を作って提示し、希望があれば旅のしおりWebページ化まで行う。しおりは trip-plan リポジトリのビルドシステム（tools/template.html＋trips/<旅名>.json＋tools/build.py）で生成するのが標準で、手書きHTMLは作らない。宿・キャンプ場は予算（1泊の総額上限）や車の横付け可否・クマ遭遇リスク・予約不要かどうか等の条件で裏取りして選ぶ。場所を地理でグループ化し、遠近を交互に置いて移動負担を分散し、各スポットが各日の曜日に営業しているかを必ず照合する。日程・フライト・宿・移動手段のいずれかが変わったときの組み直し（既存予約との日付整合・キャンセル確認を含む）にも使う。デプロイ・LINE共有・PWA・写真・天気表示などしおりの機能改修の依頼でも必ず参照する。
---

version: 2.68
last updated: 2026-08-29

# 旅行プランナー（汎用）＋ 旅のしおり制作・運用

行きたい場所・飲食店のリストと条件（日程・フライト・宿・移動手段）から、回りやすい旅程を組み立て、希望があれば「旅のしおり」Webページ（trip-plan リポジトリ）にする。
ねらいは「回りやすさ（地理）」「営業日の正しさ（曜日照合）」「安全条件の裏取り」「同じ品質で量産できる仕組み」の4点。

過去の実運用で 曜日の取り違え・定休日の見落とし・連続デプロイ中の閲覧で壊れたキャッシュが残る事故 が実際に起きた。ワークフローの順番と運用ルールを守ること。

---

## 全体の流れ（2段階）

まず段階1（テキスト計画）を完成させ、最後に一度だけしおり化を提案する。しおりはユーザーが希望したときだけ段階2へ進む（毎回作らない）。ただし、他AIからの引き継ぎ資料など計画が既に確定している場合や「◯◯のプランで作って」と言われた場合は、最新情報の裏取けだけ行って段階2から始めてよい。

```
段階1：テキスト計画
  入力確認 → 位置/営業時間/定休日の取得 → 宿・キャンプ場を条件で選定
  → 曜日算出 → 地理グループ化 → 移動手段で仕分け → 遠近交互配置
  → 曜日×営業の照合 → 時間設計 → 注意点（既存予約との整合・キャンセル確認）
  → 規定フォーマットでテキスト出力 → しおり化を一度だけ提案
        ↓（希望した場合のみ）
段階2：しおり化（trip-plan リポジトリ）
  trips/<旅名>.json を書く → tools/build.py で生成 → tools/qa.py で検査
  → tools/deploy.py で1コミットpush → Vercel自動デプロイ → BUILD_TAG確認
```

---

## 段階1：テキスト計画のワークフロー（この順番で進める）

### 1. 入力を確認する
- 行き先／日程と年／フライト・交通（時刻・便名・予約番号・人数）／宿／現地の移動手段と使える期間／行きたい場所リスト／同行者（既定は大人2＋小2＋年長の家族4人）
- 欠けていれば1つだけ質問して埋める（憶測で進めない）
- 入力は途中で変わる前提。変更が来たら曜日算出から照合までを必ずやり直し、既存予約の日付整合とキャンセル要否を確認する

### 2. 各場所の位置・営業時間・定休日を取得する
`places_search` で緯度経度・営業時間・定休日・place_idを取得。名前から場所を思い込まない（同名店・支店の取り違え事故あり）。批評データ（★・口コミ件数）も取れるので食事候補の選定に使う。

### 2-a. 情報源の優先順位（誤情報の混入を防ぐ）
- 一次情報＝施設公式サイト・自治体公式・公式SNS。NAVITIME/Yahoo!トラベル/じゃらん/いこーよ等のまとめサイトは二次情報で、施設の告知を要約する過程で条件が広がっていることがある（実例：公式「お盆期間のみ事前予約制」→まとめサイト「GW、8月、3連休は事前予約制」→しおりに「8月は事前予約制」と書いてしまった誤り）
- 公式サイトがJavaScript描画で `web_fetch` では本文が取れないことがある（例：kiso-toymuseum.com）。その場合は断定しない。「繁忙期は予約制になることがある／出発前に公式で確認」と書き、条件を広げない
- 予約・定休・料金など行動が変わる情報は、二次情報しか無いなら「〜の場合がある」と幅を持たせ、必ず公式確認を促す一文を添える
- 制約は運用の実態まで調べる（例：予約制でも当日入館可・予約客が落ち着いてから案内、など）。「入れない」と誤解させない
- ユーザーから「本当？」と指摘されたら、自分が使った情報源を明示し、公式と食い違うなら公式を正として即座に直す

### 2-b. 宿・キャンプ場を「条件」で裏取りして選ぶ
家族オートキャンプが基本。優先条件はユーザーがその都度変えるので最新の優先順位を確認する（実例：2026夏は「①クマ遭遇リスク低 → ②無料 → ③予約不要 → ④営業中 → ⑤涼しさ」に変わった。予約不要重視の理由は直前の天気で行き先を決めたいから）。
- 従来既定：1泊5,000円以下（家族総額）／車をサイトに横付けできるオートサイト
- 提案時は必ず最新情報で確認：現在も営業しているか／該当日に利用可能か／臨時閉鎖・除草作業等がないか／宿泊可か／本当に予約不要か／道路規制・増水がないか
- クマは「いない」と断言しない。「直近の目撃が少ない」「遭遇リスクが比較的低い」という評価にとどめ、県の出没警報・自治体の目撃マップ・施設公式の熊情報ページを裏取りする。クマ生息域に泊まる場合は食料・ゴミ管理を最重要とする対策noticeをしおりに入れる

### 2-b-1. クマの直近目撃地は候補から外す（必須）
- **直近1年以内にクマの目撃・痕跡・人身被害が報告されたキャンプ場および周辺（おおむね半径5km）は候補から外す。** 「対策すれば大丈夫」と条件付きで残さず、原則として提案しない
- 候補を出す前に必ず調べる：県の出没マップ・出没注意情報／市町村の目撃情報一覧／施設公式サイトの熊情報ページ／`web_search "<地名> クマ 目撃 2026"`
- 目撃時期・場所・頭数（親子か単独か）・キャンプ場からの距離を控え、判断の根拠として会話に残す
- **ユーザーが行きたい場所を挙げたときも同じ確認をする**。該当があればその場で伝える。「〇〇は今年7月に隣接する△△で親子2頭の目撃があります」のように、時期・場所・距離を添えて事実として示し、代替案を出す
- 秋（9〜11月）は冬眠前で行動範囲が広がり、木の実の不作年は市街地近くまで下りてくる。同じ場所でも季節で評価が変わるので、春夏の情報で「安全」と判断しない
- 除外の対象は宿泊地だけでなく、日中に立ち寄る山間の観光地・遊歩道・渓流も同様に扱う
- 河川敷サイトは大雨・上流の増水が最大リスク。雷雨予報なら中止判断、と明記する
- 予約した場合は予約サイトURL・キャンセルURL・料金・予約番号を会話に残す。キャンセル確認はGmailで送信者アドレス指定検索（`from:rsv@nap-camp.com` 等）で行い、キャンセル料の有無まで本文で確認する

### 2-c. 祝日の並びを必ず確認する
- `date` の曜日だけでなく**祝日と「国民の休日」を調べる**。祝日に挟まれた平日1日は休日になる（実例：2026年は敬老の日9/21と秋分の日9/23に挟まれた9/22が国民の休日となり、9/19〜23が11年ぶりの5連休。これを平日と誤認して休息日を置いた）
- 連休が判明したら、主要スポットは連休前後の平日に寄せ、連休中は拠点から動かない設計にする
- **月曜休館の施設は「祝日の場合は直後の平日が休館」**になる。祝日が続くと休館日が思わぬ日にずれる（実例：金沢21世紀美術館は9/21〜23が祝日続きのため9/24が休館。最終日に組んで閉館に当たるところだった）。公式の休館日カレンダーを必ず見る

### 3. 曜日を正確に算出する（最重要の事故防止）
必ず `date` コマンドで確認。日程が変わるたびに再実行。
```bash
for d in 20 21 22 23; do date -d "2026-08-$d" "+%m/%d (%a)"; done
```

### 4〜6. 地理グループ化・移動手段仕分け・遠近交互配置
- 同じエリア・通り道は同じ日にまとめる。遠出の日が連続しないよう近場日を挟む
- 車のない日に遠出を入れない。レンタカー受取日は開始を遅めに
- **1日の運転時間を設計指標にする**。子連れの目安は最長3時間まで、往復2時間以内が快適。長距離になる日は途中に長時間の観光を挟んで分割する（例：名古屋→敦賀2時間＋観光3時間＋敦賀→小松1時間20分）
- 長期旅行（4泊以上）は**拠点連泊型**にして設営・撤収を2回までに抑える。拠点は「行きたい場所がすべて1時間圏内に入る位置」で選ぶ。遠方の名所を1つ入れるために毎日往復するくらいなら、その名所を諦めて別の旅にするほうが満足度が高い

### 6-b. 同行者に合わせてスポットを選ぶ
- 家族連れでは**子どもが体を動かせる・触れる場所を優先**する。動物園、昆虫館、水族館、ふれあい施設、体験型ミュージアム、大型遊具のある公園
- 神社仏閣・庭園・美術館は入れすぎない。入れる場合も「任意」と明記し、疲れていたら省ける形にする
- 建物系でも「中に入れる・触れる・操作できる」ものは子ども向けとして扱える（体験型ミュージアム、洞窟、シミュレーター、遊べる現代美術）

### 7. 各スポット×曜日で営業判定する（本スキルの中心）
- 定休日照合（祝日・夏休み期間の例外に注意：「8月は事前予約制」等もここで拾う）
- 要予約・当日受付のみ・乗馬時間帯のような時間制約も明記
- 判定根拠は会話に残す

### 7-b. 道路とルートの注意を必ず調べる（クマと同じ重みで扱う）
安全と当日の判断に直結するので、クマ情報と並べて毎回確認し、その日の `.notice` に書く（移動行のnoteは廃止した）。

- **ナビの誤案内**：施設公式が「カーナビ・Googleマップが別ルート（狭い林道など）を案内する」と注意喚起していないか確認する（実例：赤沢自然休養林。国道19号で上松市街まで行き県道473号で入るのが正解）。該当があれば「正しい経路」を明記する
- **キャンプ場までの最後の数km**：Googleのレビューで路面状況を確認する。「すれ違い困難」「未舗装で車の底を擦る」「ガードレールがない」といった記述は高確率で本当（実例：ひとやま一里野は複数レビューが悪路を指摘）
- **高速道路の工事・規制**：リニューアル工事の車線規制や夜間通行止めを出発前に確認（実例：北陸道は2026年度に敦賀〜木之本などで工事期間あり）
- **連休の渋滞**：下りは初日の午前、上りは最終日の午後がピーク。日曜夕方の東名阪（四日市付近）は定番。出発時刻の目安を日別noteに書く
- **山間部の給油**：ガソリンスタンドが少なく日曜・夜間は閉まる。山へ入る前に満タンにする旨を書く
- **雨天時の道路**：河川沿いの道は増水・冠水、山道は落石。雨の翌日は川遊びだけでなく道路状況も判断材料にする
- **冬季閉鎖・時間帯規制**：季節道路（白山白川郷ホワイトロード等）は開通期間と通行時間を確認する

### 8〜9. 時間設計・注意点
- **各行に予定時刻を入れる**（`rows[].plan`）。施設の営業時間とは別バッジにして、左＝自分たちの予定・右＝営業時間で並べる。旅行中に遅れているかどうかを判断できるようにするため
- **出発は早くても9時**。早朝発は家族の負担が大きく、実際に却下された。連休初日でも渋滞のピーク（早朝〜午前）を少し外せる利点がある
- **キャンプ場の受付時間から逆算する**。チェックイン可能時刻（例：15:00）と受付終了時刻（例：17:00）は別物。受付を過ぎると管理人が不在になる施設が多い。到着予定を受付終了の1時間前に置き、途中の観光の終了時刻を決める（実例：西俣は受付17:00までなので、敦賀を13:30に出る必要があった）
- **入浴とドライヤー**：キャンプ場のシャワー室にコンセントが無いことが多い。髪を乾かすなら日帰り温泉のドライヤーを使う前提で、入浴を行程に組み込む
- 到着日は軽め、出発日は締切から逆算。日曜夕方の帰路渋滞（東名阪・四日市等）も注記
- 宿の空白／予約変更・キャンセル要否／詰め込みすぎの日の「現地で取捨選択」注記
- 2案で迷っている場合は比較表（クマ安心度・涼しさ・夜の気温・費用・観光）と過去キャンプの体感気温比較を出して判断材料にする

### テキスト出力フォーマット（厳守）
全体をコードブロックで囲む／行頭は実際の「・」／日付見出しは【】／矢印は使わない／各日の前に空行。最後にしおり化を一度だけ提案する。

---

## 段階2：しおり制作（trip-plan リポジトリのビルドシステム）

### リポジトリ構成（2026-08時点・確定）
```
taka-a1111/trip-plan（Vercel自動デプロイ、本番 https://trip-plan-beryl-phi.vercel.app）
├ index.html                 … カレンダー・旅一覧のトップページ
├ <旅名>.html                … 各旅のしおり（kiso / ise / okinawa / wakayama …）
├ manifest.json / sw.js / icons/ … PWA一式
├ assets/hero_<旅名>.jpg / day_<旅名>_<n>.jpg … 写真素材（ビルド時にbase64埋め込み）
├ trips/<旅名>.json          … 旅データ（この編集が制作作業の本体）
└ tools/
   ├ template.html … 全旅共通テンプレート（CSS・天気・地図・LINE対策・PWA登録）
   ├ build.py      … JSON＋テンプレート → <旅名>.html
   ├ qa.py         … 1000/430/380px検査（横スクロール・地図数・JSエラー）
   ├ deploy.py     … 複数ファイルを1コミットでpush（Git Data API）
   └ README.md     … 使い方
```

### 制作の標準手順
1. `curl https://raw.githubusercontent.com/taka-a1111/trip-plan/main/...` で現状を取得
2. 新しい旅＝trips/kiso.json をコピーして書き換えるだけ。HTMLを直接書かない・直接編集しない（共通修正は template.html を直して全旅を再ビルド）
3. `python3 tools/build.py <旅名> <BUILD_TAG>` → `python3 tools/qa.py <旅名>.html`
4. index.html の `var trips=[...]` に旅を追加（name / s / e / a / color / thumb=480px幅base64）
5. `GH_TOKEN=... python3 tools/deploy.py "メッセージ" file1 file2 ...` で必ず1コミットにまとめてpush
6. 30〜60秒後にハードリフレッシュしてBUILD_TAGを確認するようユーザーに案内
7. **修正・デプロイしたら必ず本番URLを添える**（`https://trip-plan-beryl-phi.vercel.app/<旅名>.html`）。ユーザーがすぐ開けるようにするため、報告だけで終わらせない

### trips/<旅名>.json スキーマ（要点）
- `title / h1_top / h1_sub / dates_label / date_s / date_e / hero_image / credit`
- `wx`：既定の天気地点 `{lat,lon,jma(気象庁offices),tk(tenki.jpパス),nm}`。宿が移動する旅は各日に `days[].wx` で上書き（tenki.jpパスは `web_search "tenki.jp ◯◯町の天気"` でURLから裏取り。推測しない）
- `points`：`{key:{n,lat,lon,k(spot|stay|meal),q(Googleマップ検索語),official?,reserve?}}`
- `days[]`：`id/dd/dw/badge/tags[[t-クラス,ラベル]]/theme/notice/rows/pins/photo?/meals?/wx?`
  - rows：`{"type":"move","name","dur",badges?,note?}` ／ `{"type":"spot","key","cat","cat_lbl","time","badges":[[bクラス,文言]],"note"}`
  - pins：地図に載せる順のkey列。食事候補は `{"key":k,"lb":"A"}`（オレンジピン）
  - meals：`[{"slot":"昼|夜","items":[{"lb","name","q","meta(★・営業・ひとこと)"}]}]` → 「食事の候補」ブロックとして描画
- `rt_order`（概観地図・ヒーローのスポット数の元）／`visit_order`（Googleマップ経由地リンク、最大8）／`memo`（末尾の費用目安・要確認事項）

### デザイン・内容ルール（確定事項）
- 写真：ヒーロー1枚＋各日1枚の帯写真（案A）が標準。日の帯は幅880px・JPEG品質55・30〜50KB目安、右下に小さくクレジット。CC/CC0/パブリックドメインのみ（Openverse APIで検索、CC BY/BY-SAは作者+ライセンスを必ず表記）。実在スポットの写真が無ければ汎用写真に「イメージ」と明記。Googleマップ・公式サイトの写真は転載禁止。旅行後は自分たちの写真に差し替えれば権利問題ゼロ
  - Wikimedia原寸のDLは説明的User-Agent必須（デフォルトUAは403）。OpenverseのURLをそのまま使い、404なら消えた画像なので別candidateへ
  - **日別写真は1日1ファイル。同じファイル名を複数の日で共有しない**。共有していると、あとで片方の日の写真を差し替えるつもりで上書きしたとき、無関係な日の挿絵まで入れ替わる（実例：`day_hokuriku_5.jpg` を2日目と5日目で共有していて、5日目をうさぎの写真に差し替えたら「キャンプ場だけの日」にうさぎが出た）
  - **日程からスポットを外したら、その日の写真も一緒に見直す**（実例：山中温泉を外したのにこおろぎ橋の写真が最終日に残っていた）
  - 差し替え後は必ず全日の写真を機械点検する：
    ```python
    files=[d.get('photo',{}).get('file') for d in data['days'] if d.get('photo')]
    assert len(files)==len(set(files)), '写真ファイルの重複'
    assert all(os.path.exists(f) for f in files), '写真ファイルの欠損'
    ```
    加えて、差し替えた日だけでなく**同じファイルを参照していた日も目視で確認する**
- カテゴリは固定7種＋食事候補：観光/遊ぶ/グルメ/買物/温泉(#955d84)/泊まる/移動。凡例と色タグを一致させる。装飾アイコン禁止
- 天気の判定は「気象庁の公式予報を主、数値モデルを従」。府県天気予報（`bosai/forecast/data/forecast/<office>.json`）の天気コードと予報文を土台にし、降水確率も気象庁の値を優先する。数値モデル（Open-Meteo）は補助
  - **細分区（`wx.jarea`）を必ず正しく設定する**。既定は先頭エリアなので、指定しないと別地域の予報を拾う（例：長野県は北部/中部/南部で木曽は「中部」、三重県は「北中部」、石川県は「加賀」、沖縄は「本島中南部」）
  - 雷マークは「気象庁の予報文に『雷』の語がある」または「複数モデルが雷雨コードで一致」したときだけ。**1モデルだけの雷では出さない**（GFSは対流性コードを過剰に出す傾向があり、全日が雷マークになる事故が起きた）。1モデルのみの場合はポップアップに「一部の予報モデルは雷雨を予想」と注記する
  - 気象庁が「くもり」の日は確率が中程度でも曇りアイコンを維持する（rain は確率70%以上、shower は50%以上）
- アイコンは形と色で判別できること。曇りは薄いグレーの雲のみ、雨は青い雨粒、雷雨は**濃いグレーの雲＋大きな稲妻（白フチ付き）**、快晴は大きな太陽。同じ雲に小さな飾りを足すだけの差分にしない
- 日ヘッダの天気チップは地名なしのコンパクト表示（アイコン＋最高/最低＋◯%）。**「降水」の語は付けず数値のみ**。380px幅で必ず日付と同じ行に収まること（qa後に `.day-date` と `.wx` のtop差<20pxを機械チェック）。余白があるならチップの文字は大きめにする（0.95rem程度）
- **予報が出るのは直近だけ**。気象庁の週間予報は7日先、Open-Meteoは16日先まで。1か月先の旅で天気が出ないのは正常なので、聞かれたら「◯月◯日ごろから表示される」と答える
- **予報が出るまでは「平年」を出す**。Open-Meteoのアーカイブ（`archive-api.open-meteo.com`）で過去5年の同じ月日の最高・最低気温を平均し、`days[].wxn={hi,lo}` としてJSONへ焼き込む。テンプレートは予報が取れない期間だけ、いつもの天気チップの位置に「平年 28°/22°」を出す（グレーの平年バッジ付き）。終了した旅には出さない（そちらは実績値 `wxp` の担当）
- 移動行に地図リンクを付けない。フライト行の予約番号・締切だけは `"keep_note": true` を付けてnoteを残す（→§移動行）
- 地図リンクは施設名検索形式のみ（座標/cid禁止）
- **公式URLはスポットを追加するたびに必ず調べて入れる**。推測でURLを書かないのは当然だが、「調べずに省く」のも不可（実例：北陸プランで13スポット分の公式リンクが抜けたまま公開されていた）
  - 探す順序：施設の独自ドメイン → 運営者の公式ページ → 自治体・観光協会の公式ページ。SNSしか無い場合はSNSでもよい
  - 独自ドメインが見つからないときは「公式サイトなし」と会話で伝える。黙って空欄にしない
  - 共同浴場や道の駅は独自サイトを持たないことが多い。自治体の観光公式（こまつ観光ナビ等）や運営法人のページで代用する（実例：粟津温泉総湯＝小松市公式、山代温泉総湯＝加賀温泉郷公式、西圓寺温泉＝佛子園公式）
  - **裏取りで元の記述が誤っていたら即座に直す**（実例：せせらぎの郷は電話番号が別施設のもので、料金も公式と違っていた。公式で「月曜が祝日なら翌火曜が休業」と確認できたので、「電話で確認を」ではなく断定に変えた）
  - 裏取りの過程で料金・休館日・利用制限が判明したらnoteも同時に更新する（実例：リラ・ポートは土日祝850円かつ刺青不可、三国ゆあぽーとは第3水曜休館）
- **`.notice` は2文以内**。それ以上の情報（場内ルール、料金の詳細、動線の補足）は該当するスポット行のnoteへ移す。旅行中に読み返すことを前提に短くする
### スポット行のレイアウト（2026-08-26 確定）
- 行は「見出し行＋本文」の2段構成にする。見出し行は左から **予定時刻（`rows[].plan`）／カテゴリ色の番号丸／スポット名／今日の目玉／カテゴリチップ**。本文（説明・項目表・地図リンク）は**行の全幅**を使う
- 予定時刻と番号丸はスポット名に対して**縦方向センタリング**する（名前が2行に折り返しても中央に来る）
- **見出し行は「予定時刻／番号／スポット名／今日の目玉／カテゴリチップ」を全部おなじ高さの中心にそろえる**（`align-items:center`。ベースライン揃えにしない）
  - 開閉の矢印は `position:absolute` で右端に置く。flexの流れに入れると、あと数pxの不足でチップだけが次の行へ落ち、時刻と番号が名前より上にずれて見える（実機で指摘あり）
  - 「今日の目玉」とカテゴリチップは `.s-meta` で1つにまとめ、離れないようにする
  - 見出し行は `flex-wrap:nowrap`。名前とチップは必ず同じ行に置き、幅が足りないときは名前のほうを折り返す
  - **閉じたカードの高さは全スポットで同じにする**。見出し行に `min-height:calc(1.08rem * 1.42 * 2)`（名前2行ぶん）を持たせ、名前が1行の行も2行の行も同じ高さにする。高さがばらつくとリストが不揃いに見え、日ごとの分量も比べにくい
  - 目玉の行だけ名前を大きくするのはやめた（星マークと番号のリングで足りるうえ、1pxだけ高さがずれてカードがそろわなくなるため）
  - 名前が3行になるとこの高さに収まらない。`points[].n` は施設名だけにするルールを守れば3行にはならない（実例：白浜の「グランパスへ戻って 場内温泉 ＋ バーベキュー」が3行で1枚だけ背が高くなっていた）
  - スポット名は `word-break:keep-all` ＋ `overflow-wrap:anywhere`。空白のある名前は語の切れ目で折り返し（「三国温泉 ゆあぽーと」→「三国温泉／ゆあぽーと」）、空白のない長い名前は従来どおり折り返す
- **`points[].n` は施設の名前だけにする**。「（日帰り温泉）」「（森歩き・清流あそび）」「（夜の散策・買い物）」のような用途や中身の説明、「（那覇・牧志）」「（本部町）」のような地名、「（泊）」「（設営）」のような役割をかっこ書きで足さない。用途はカテゴリチップ、内容は説明文、場所は地図リンクで分かるので、名前に入れるとスマホで2行に折り返して読みにくくなるだけ
  - 例外は施設の正式名称にかっこが含まれる場合だけ。サイト区分や部屋番号のような予約情報は名前ではなく項目表へ書く（実例：西俣キャンプ場の「フリーサイトG」は `予約` の項目へ移した）
  - 同じ理由で、移動行の `name` にも説明のかっこ書きを付けない（「朝食・撤収（雨がやむのを待って）」など）
- **地図・公式・予約のリンクはカードの表（常に見える場所）に横並びで置く**。見出し行のすぐ下、名前と同じ列にそろえる。閉じたままでもその場所をすぐ調べられるようにするため（本文の中に隠すと、調べるのに一度開く手間がかかる）。右端に縦積みするのは本文幅を80pxほど奪うので不可
  - **キャンプ場は「公式」と「なっぷ」を両方出す**（`points[].official` と `points[].nap`）。空き確認はなっぷ、料金とルールは公式、と使い分けるため。なっぷは施設ページのURLを入れる（トップページのURLは不可）。独自の予約サイトを使う施設は `reserve` で「予約」ボタンにする（実例：やぶっちゃは select-type）
- **カードの表に家族4人ぶんの料金を1行だけ出す**（ボタンの下）。`fields` の `料金` から 👪 の行を自動で抜き、無ければ「無料」を拾う。`rows[].fee` を書けばそちらが優先される
  - 「ほど」やかっこ書きは落として金額だけにする。行の高さは固定にして、料金のないスポットでも高さがずれないようにする
  - 詳細（大人・子ども・駐車場の内訳）はこれまでどおり項目表に置く。表に出すのは総額だけ
- **開閉の当たり判定はカード全体**にする（`.spot-row` にクリックを付ける）。見出し行だけだと、カードの余白や本文をタップしても反応せず、矢印を狙わせることになる
  - リンクとボタン（`a,button,input,label,select,textarea`）の上では開閉しない。文字を選択している最中も開閉しない
- 時刻を持たない旅（過去の沖縄・白浜など）は時刻レールが自動で畳まれる
- **移動行は極力薄く小さく**する（背景なし・枠なし・文字0.82rem・上下パディング3px・点は7px）。所要のバッジも背景を外して文字だけにする。主役はスポットのカードで、移動は行間の細い注記として見えるのが正しい
- **施設と施設のあいだには必ず移動行を入れ、距離と所要だけを書く**（`name` に「約12km」、`dur` に「約20分」）。前後のカードを見れば区間は分かるので、地名は繰り返さない。ただし**その日の最初の移動だけは出発地を書く**（「西俣キャンプ場から 約70km」）。宿は日の最後のカードなので、朝どこから出るのかが分からなくなるため。1区間でも欠けると、書いてある区間と無い区間が混ざって読みにくい。距離と所要はOSRM（`router.project-osrm.org/route/v1/driving/`）で実測し、市街地ぶんを1.15倍して5分刻みに丸める
- **移動行に説明文（note）を書かない**。出るのは「予定時刻／区間／距離／所要」だけにする。渋滞・出発時刻・買い出し・撤収の段取りといった判断はその日の `.notice` へ、施設の受付時間やチェックアウト時刻はスポット行の項目表へ移す。移動行に文章を足すと、スポットのカードより先に長い文が目に入り、どこへ行く日なのかが読み取れなくなる（実機のスクリーンショットで指摘あり）
  - 例外はフライト・レンタカー返却など、予約番号とチェックイン締切を残す行だけ。JSONの移動行に `"keep_note": true` を付けたときだけ `build.py` がnoteを描画する
  - 移動noteを消すときは、そこにしか書いていない情報がないか必ず確認してから消す（実例：北陸の最終日は「福井ICでソースカツ丼に寄るなら30分」がnoteにしかなく、noticeへ統合してから削除した）
- **1スポット＝1枚のカード**にする（白背景・角丸12px・1pxの枠・薄い影）。カード同士は10pxあけ、移動行はカードにせず隙間に置く細い行にする。どこまでが1つのスポットかを目で追えるようにするため
- カードの**左端4pxをカテゴリ色**にする（`row-<cat>`）。番号丸と同じ色なので、色だけで種類が分かる
- **地図のピンもカテゴリ色に揃える**（観光＝ティール／遊ぶ＝コーラル／グルメ＝ゴールド／買物＝スレート／温泉＝#955d84／泊まる＝濃紺）。`points[key].c` にカテゴリを持たせ、ピンに `mk-c-<cat>` を付ける。行の色と地図の色が違うと対応が取れない
- 項目表は**ラベルを行頭に置く段落型**にする（ラベル専用の列を作らない）。ラベルは小さめの太字でカテゴリ色、値はそのすぐ後ろから全幅で流す。項目のあいだは点線で区切る。列を切ると値の幅が5emぶん狭まり、折り返しが増えて縦に伸びる
- **`.fv` を `display:flex` にしない**。縦積みのフレックスにすると、値の中の絵文字・記号・文字がそれぞれ別の行に落ちる（🅿️／◯／無料 が3行になる事故）
- 値の中の小項目（`.amn`）は**1行1項目を基本**にする。ただし「🔥 直火 ✕　🎆 花火 ✕」のように**2つ並べても1行に収まるものはまとめる**。1つで1行が埋まらないスカスカの行を作らない
- 補足の一言（`.fnote`）は**必ず改行して**値の下に置く。前の項目の末尾にくっつけない
- 文字サイズの下限：項目表0.90rem／店名0.96rem／一言0.92rem／スポット名1.08rem。補足文にも `--ink-faint`（#8aa0a6）のような薄いグレーは使わない（実機で読めないという指摘あり。#4d6168 程度まで濃くする）

### 項目表（`rows[].fields`）
文章で書くと読み返しにくい情報は、ラベルと値の表にする。値はグリッドの右カラムに入り、ラベルは縦に揃う。
- 書式：`"fields": [["料金","大人600円ほど"], ["営業","9:00–17:00・無休"], …]`
- カテゴリ別の標準セット
  - 泊まる（キャンプ場）：受付（IN・OUTも含める）／料金（🅿️も）／予約／ルール（ゴミ・禁止事項・乗り入れ）
  - 温泉：料金（🅿️も）／営業／定休／**備品** の4つ
  - 観光・遊ぶ：料金（🅿️も）／営業／定休 の3つ（**同じカテゴリなら並び順も文言も必ず同じ**にする）
  - 温泉の備品は**絵文字＋記号で1行ずつ**、この順で書く：`🧼 せっけん・ボディソープ`→`🧴 シャンプー・リンス`→`🌬️ ドライヤー`。ドライヤーの絵文字は存在しないので🌬️で代用する。記号は色分け（◯＝ティール／✕＝朱／△＝金）し、補足は下に小さく1行
  - 温泉の**備品は必ず調べて書く**：ボディソープ・石けん／シャンプー・リンス／ドライヤーの有無と、有料か無料か。共同浴場はアメニティなしが多く、ドライヤーが有料（1分10円など）の施設もある。持参の要否が変わるので省略しない
  - **電話番号は必ず `tel:` のリンクにする**（`<a href="tel:0761678100">📞 0761-67-8100</a>`）。運転中や受付に遅れそうなときにタップで発信できるようにするため
  - 宿の受付は `🕐 受付 時間帯`／`🛎️ IN 時刻　🚪 OUT 時刻`／`📞 電話` の3行にする。「管理棟で」「管理人は」のような説明は削る
  - 料金は**絵文字で行を分ける**：`👨‍🦰 大人の単価`／`🧒 子どもの単価（年齢や学年は括弧内に書く。例：150円（小学生）、150円（6〜12歳））`／`👪 家族4人の合計`
  - **駐車場は独立した項目にせず、料金の最終行に🅿️付きで足す**。**あるときは金額だけ**（`🅿️ 無料`／`🅿️ 1回500円ほど`）、**ないときだけ✕**（`🅿️ ✕`）。行を1つ節約でき、お金の話がまとまる
  - ゴミは**回収があるか持ち帰りかだけ**書く（「（獣害対策）」のような理由は不要）
  - キャンプ場のルールも絵文字で1行ずつ：`🚯 ゴミ`／`🔥 直火`／`🎆 花火`／`🐕 ペット`／`🔇 夜間`。**1行に詰め込まず改行する**（`white-space:nowrap` にすると横スクロールが出る）
  - 補足の一言（`.fnote`）は**必要なときだけ**。ボディソープがあるのに「持参」と書くような、記号と矛盾する説明を残さない
  - 旧: 駐車場は無料か有料かを先に書く。有料なら料金（例：休日は最初の30分350円、以降30分ごとに150円）。台数は補足程度でよい
  - グルメ：営業／定休
- **項目は必要最小限にする**。数字で答えが決まるもの（料金・営業・定休）だけを表に置く。眺め・見どころ・注意・設備・駐車場のような文章は、名前の下の説明文へ移す
- **所要・移動・昼食は項目に入れない**。所要は予定時刻（`rows[].plan`）から読み取れ、移動は移動行に、食事は「食事の候補」の表にある
- **予約番号は書かない**。「どこで予約したか」が分かればよいので、`なっぷ`・`楽天トラベル` などの予約先名にリンクを張る（値にそのまま `<a>` を書ける）。番号は控えとして会話とメールに残す
- **一言の解説は必ず残す**。「そこが何で、なぜ行くのか」は項目に落とし込めない。項目表の上に置き、**1〜2文まで**にする。長くなるなら削る。数字（料金・時間・距離）は項目へ移して二重に書かない
- **重複を作らない**。営業時間・定休・料金を項目に入れたら、名前の横の楕円ピルからは消す。`fields` があるとき営業時間ピルは build.py が自動で出さない。ピルに残すのは項目に無いもの（「2泊目」「任意」など）だけ
- 日の先頭の `.notice` も同様に、スポット行と重なる記述は削って**日をまたぐ判断だけ**を1〜2文で書く

### 食事を含む項目には実店舗名とリンクを入れる
「昼食：商店街で海鮮」のような書き方はしない。値をリストにすると店名がGoogleマップリンクになる。
```json
["昼食", [{"name":"竹田亭","q":"竹田亭 東尋坊","meta":"★4.0・口コミ90件｜8:30–17:00｜ミニ丼＋ラーメンのセットが1,400円ほど"}]]
```
- `places_search` で★と口コミ件数、営業時間を取り、**その日の曜日に営業しているか**を確認してから載せる。3店ほどが目安
- **口コミ300件以上の店だけを載せる**。件数が少ない店は評価が偏りやすく、家族4人で外すと痛い。条件を満たす店が1〜2軒しかない地域なら、無理に3軒に増やさずその数で出す（実例：東尋坊周辺で300件を超えるのはやまに水産だけだった）
- 昼と夜をそれぞれ**6軒ずつ**、日ごとの「食事の候補」ブロックに置く。
- **キャンプ旅は夜の下に「買い出し」の枠を足す**（スーパー2〜3軒、同じ表形式）。バーベキューをする日があるため。店名にキャンプ場までの所要か立地を添え、閉店が遅い店を1軒は入れる。予算列は「—」でよいキャンプ旅で自炊が基本の日も、外で済ませる選択肢として夜を入れておく
- **表で出す**。列は「チェック／◯◯の候補／口コミ／予算（1人あたり）／営業時間／定休日」で、1列目の見出しは枠に合わせて「昼の候補」「夜の候補」「スーパーの候補」と書き分けるで、1店1行に収める（各セルは折り返さない）。
- チェックした店は**その日の地図にピンが立つ**（`la`/`lo` をJSONに持たせる）。ピンは枠ごとに色と字を変える：昼＝金の「昼」／夜＝コーラルの「夜」／スーパー＝グレーの「買」。**チェックしても地図の表示範囲は動かさない**（寄せると他のスポットとの位置関係が見えなくなる）。ピンは最前面に描いて隠れないようにするだけにとどめる。候補を全部ピンにすると1日15本で読めなくなるので、選んだものだけ出す。外せば消える
- **日別地図は食事の表より前に置く**。スマホで画面上部に貼り付ける（`position:sticky`）のは、**食事の候補（昼・夜・買い出し）を開いているあいだだけ**にする。表をスクロールしながらピンを見るための機能なので、閉じているときまで追従すると、スポットを読んでいる最中に地図が画面上部を占領してしまう
  - 実装：既定は `.daymap{position:relative}`。`.mwrap` の開閉に合わせて JS が親の `.day` へ `mealopen` を付け外しし、`.day.mealopen .daymap` のときだけ sticky にする。`static` にはしない（Leafletのレイヤーが `overflow:hidden` の外へ出て横スクロールが出る）
  - 開いた直後は `window.__refitDayMap` を呼んで地図を貼り直す
- 日ごとに「**この日の動きを地図で開く**」ボタンを置く。Googleマップの経路URLで開き、`waypoints` は店名の文字列でよい。実際の所要と距離はGoogleに計算させる
- 出発地は**前日の宿泊地**、到着地は**その日の宿泊地**にする（初日は自宅発、最終日は自宅着）。宿泊地はその日の最後の `stay` 行から取る。「1日目の最初のスポット」を起点にすると最終日が宿から始まらずズレる
- 左端のチェックは**行き先の仮決め**用。`localStorage`（キー `meal_<旅名>_v1`）に旅ごとに保存し、チェックした行は背景を色付けして太字にする。
- チェックすると**他の日に出ている同じ店をその場で薄く**する（行に `data-shop` を持たせ、選択済みと一致する未選択行に `.dim` を付ける）。同じ店を続けて選ばないようにするため。外せば即座に戻る
- **同じチェーンの別店舗もまとめて薄くする**。JSONに `br`（ブランド名）を持たせ、`data-shop` は `br` があればそれを使う（実例：8番らーめんの小松店と城南末広店、ヨーロッパ軒の敦賀本店・岡山店・総本店、サイゼリヤの各店）狭い画面では横スクロールさせる（`.mtable-wrap` の `overflow-x:auto`）。文章で並べると比較できない
- 予算は**1人あたりの目安**。看板メニューを1品頼んだときの実勢価格で書き、公式に価格表がある店はそれに合わせる
- JSONの書式：`meals:[{"slot":"昼","items":[{"name","q","bg","rv","hr","off","ft"}]}]`。`bg`は予算、`rv`は「★4.2・1,772件」、`off`は定休日、`ft`は一言
- **動線で選ぶ**。昼はその時間にいるスポットから車10〜20分以内、夜はその日の最後のスポットから宿・キャンプ場へ戻る道の途中に置く。評価だけで選ぶと逆方向の店が混ざる
- **ご当地グルメを各日に1つは入れる**（実例：福井＝ソースカツ丼・越前おろしそば、金沢＝金沢カレー、小松＝小松うどん、北陸＝8番らーめん）
- **ご当地グルメと有名店は1日で終わらせず、2日以上に載せる**。初日に食べ逃しても取り返せるようにするため。動線が合う別の日に、同じ系列の別店舗でもよい（実例：ソースカツ丼のヨーロッパ軒を初日は敦賀の2店、最終日は帰り道の福井市・総本店で再掲）
- 帰路に寄り道させるときは、その日の `.notice` に**余分にかかる時間**を書く（例：福井ICで降りるなら30分みる）
- **ジャンルを散らす**。件数だけで選ぶと回転寿司ばかりになる。ラーメン・町中華・洋食・定食・とんかつを混ぜ、同じジャンルが連日にならないようにする
- **地元の名店を優先する**。ただし6軒を口コミ300件以上で埋められない枠は、**ファミレス（サイゼリヤ・ガスト・ジョリーパスタなど）で補ってよい**。子ども向けメニューと駐車場があり、遅い時間でも入れるので保険になる。ご当地チェーン（8番らーめん、すし食いねぇ！など）は土地の味なので優先枠に入れてよい
- 例外：スポット自体が飲食店の行、および館内の食堂で完結する施設（温泉・見学施設のフードコート等）。この場合は外の店を挙げず「館内で済ませられる」と書く

- **その日の主役スポットを1つだけ強調する**（`rows[].main`）。番号のリングと薄い背景に加えて、名前の右に**星マーク**（コーラル色の★を薄い丸地に載せた22px）を置く。「今日の目玉」という文字バッジは廃止した：スマホでは横幅を70pxほど食い、名前かカテゴリチップが次の行に落ちて見出し行が2段になっていたため
  - 星には `aria-label="今日の目玉"` と `title` を付け、読み上げと長押しで意味が分かるようにする
- **ヒーローのスポット数は観光スポットのみ**を数える（泊まる・移動・温泉は除外）。全日に温泉を入れると数字が実態から乖離する
- **日帰り温泉は全日に1か所ずつ**入れる（ただし**ホテル泊の日は入れない**。大浴場かユニットバスがあるので二重になる）。評価と料金の兼ね合いで選び、家族4人3,000円以内を目安に。定休日が重ならないよう日ごとに割り当て、同じ施設が連日にならないようにする
- **末尾のメモは「そこにしか書いていないこと」だけ**にする。温泉の一覧・料金・予約番号など、スポット行や項目表に載っているものは全部削る。残すのは出発前の確認、安全（クマ・増水）、外したスポットの扱いなど
- **地図のコンテナを `position:static` にしない**。Leafletの内部レイヤーは絶対配置なので、静的にすると `overflow:hidden` の外へ出て28,000px級の横スクロールが出る。スティッキーを解除したいときは `position:relative` にする
- 日付バーの右端に**雨天の候補へ飛ぶチップ**を置く（日付チップとは色を変える）
- **雨の日の候補をページ末尾に置く**（`data.rain`）。自宅から一番遠いスポットまでの間にある**室内施設を10か所ほど**、地図のピン付きで。行き先を丸ごと振り替えられるようにするのが目的
- **雨の日の候補は各日の「食事と買い出しの候補」の下に置く**（`days[].rain` に `data.rain` の1始まりの番号を5件）。ページ末尾にまとめる方式は廃止した：雨の朝に「今日どこへ振り替えるか」を見たいので、その日の中にあるほうが早い
- **日別地図の表示範囲には、その日の食事・買い出し・雨天の候補をすべて含める**（`window.__fitDayMap`）。スポットだけに合わせると縮尺が寄りすぎて、チェックしたピンが画面の外に出てしまう（実機で指摘あり）
  - 緯度経度が 0/0 の行（旧形式で座標を持たない食事候補）は範囲の計算から外す。含めるとアフリカ沖まで引っ張られる
  - 見た目と操作は食事の候補と同じ。折りたたみ（既定は閉じ）・同じ表（室内の候補／口コミ／料金／営業／定休）・チェックでその日の地図にピンが立つ。開いているあいだだけ地図が追従する
  - **ピンはA〜Eの英字**にする。スポット行の番号ピンと数字がぶつかって見分けられなくなるため。色は藍（#4f63a8）でカテゴリ7色とも重ならない
  - その日に実際に行くスポットと同じ施設を候補に入れない。日をまたいで同じ施設が出るのは構わない
  - 日付バーの「雨天候補」チップとページ末尾の雨セクションは削除した
- **服装の目安は「昼／夜／寝」の3語だけ**にする（`wear.day` / `wear.night` / `wear.sleep`）。「半袖＋薄手の長袖」「長袖＋薄手のフリース」「3シーズン用の寝袋」のように、持ち物を選ぶのに必要な一言で止める。気温や理由の説明文はヒーローの気温チップと天気ポップアップにあるので書かない
- `.notice` は判断材料だけ（クマ対策・ナビ注意・増水・要予約・混雑・最終判断の段取り）。変更履歴やスポット行の重複は書かない
- 天気ポップアップはリンク3本（気象庁／tenki.jp／ウェザーニュース）と統合値1行のみ。各サイトの数値を横に並べる案は却下済み：tenki.jp・ウェザーニュースはAPI非公開で数値を取得できず、気象庁APIだけ実装しても代表地点（例：長野市・標高360m）の値になり、ヒーローのピンポイント値（標高1,300m）と10℃近くずれて紛らわしい。3つ揃わないなら入れない
- 天気はHTMLに焼き込まず、開くたびにAPIから再取得（固定ではない）。ユーザーに聞かれたらそう説明する。旅行後は対象期間外になるので自動的に消える
- **ヘッダーは「戻る／旅名／共有／メニュー」の4要素**にする（高さ57px）。左端は `←`（`index.html` へ。`history.back()` は使わない。LINEから直接開くと戻り先がないため）、中央は旅名と日付（`{{NAV_TITLE}}`＝`h1_top` の先頭語、`{{NAV_DATES}}`＝`date_s`–`date_e` から自動生成。旅名は `nav_title` で個別指定もできる）、右は共有とハンバーガー
  - **タイトルは「旅のしおり」ではなく旅名にする**。ヒーローはスクロールで消えるので、どの旅を見ているかが常に分かる場所が必要
  - **ボタンは枠・背景・影を持たせない**。40pxのタップ領域は保ったまま、線画のアイコンだけを地に置く。枠付きの白ボタンが3つ並ぶと本文より目立ってしまう
  - ナビ（今日の宿へGoogleマップで案内。日付から当日の `stay` を選び、旅行期間外は初日の宿）はメニューの中に入れる。共有（`navigator.share`、非対応ならURLをコピー）は家族へ送る頻度が高いのでヘッダーに出したままにする
  - ナビの高さを変えたら `html{scroll-padding-top}`、日別地図の `position:sticky` の `top`、日付バーのスクロール量（`absTop(d)-72`）をそろえて確認する
- **ハンバーガーメニューに日程リンクは入れない**（画面下の日付バーと重複する）。入れるのは1画面では届かない場所：全体地図・天気・持ち物リスト・予約と確認事項・カレンダーへ戻る
- **右下のピル型ボタン（今日のしおりへ）は付けない**。かつて実装して却下された。残すのは「スクロール後に出る右下↑」だけ
- **画面下の日付バーは採用する（2026-08-26に方針変更）**。以前ホームアイコン付きの版が却下されたが、縦に長くなった対策として日付だけの版を入れ直した。仕様は下記
  - 日付＋曜日のチップを横並び。タップでその日の見出しがヘッダー直下に来る位置までスクロールする
  - スクロール位置に応じて今見ている日を自動でハイライトし、そのチップがバーの中央に来るよう寄せる
  - ヒーローを抜けたあたり（scrollY>260）から現れる。最上部では出さない。日数が多い旅は横スクロール
  - 曜日は色分け：**日曜・祝日＝赤（#d94f3d）／土曜＝橙（#e0862a）／平日＝グレー**
  - 祝日は `build.py` の `holidays(year)` が計算する（固定祝日、ハッピーマンデー4種、春分・秋分、**振替休日**、**国民の休日**）。手入力しない
  - 右下↑ボタンはバーに重ならないよう `bottom` を上げる。`body` に下余白を確保する
- **旅行期間中はページを開いた時に当日の先頭へ自動移動する**。URLに `#dayN` がある場合はそちらを優先し、読み込み直後にユーザーが指で動かしたらキャンセルする。位置は `offsetTop`（`reveal` の transform に影響されない）から計算し、150ms と 800ms の2回で合わせる
- **食事と買い出しの候補もブロックごと折りたたむ**（既定は閉じ、見出しだけ）。18軒＋スーパー3軒で1日3,000px近くになり、開いていると旅程が追えなくなるため
- **スポットは1つずつ折りたたむ**（既定は閉じ、見出し行だけ表示）。閉じた状態では「予定時刻／番号／スポット名／今日の目玉／カテゴリ」が並び、その日どこへ行くかを一覧できる。タップで説明・項目表・リンクが開く。移動行は畳まない
  - 各日の右上に「すべて開く／すべて閉じる」を置く。開いたら `__refitDayMap` で地図を再フィットさせる
- **日ごとのアコーディオン（日単位で全部畳む案）は却下**。既定で当日以外を畳む案を実装して縦を67%短縮できたが、全日が開いている方がよいとの判断で撤去した。長さ対策は日付バーで行う
- **持ち物リストに重複を作らない**。「着替え」と「子どもの着替え多め」、「タオル」と「大きめタオル」のように同じものを別のカテゴリに置かない。テンプレート共通なので、追加する前に全カテゴリを見て重ならないか確かめる
- キャンプ旅では**トイレ・ごみの枠**を持つ（ポータブルトイレ、消臭剤、防臭袋、トイレットペーパー、ゴミ袋）。ゴミ回収のないキャンプ場が多く、持ち帰りが前提になるため
- **ドライヤーは日帰り温泉のものを使う前提**なので持ち物には入れない。リストの説明文にその旨を書く
- **持ち物リストは既定で折りたたむ**。出発前しか見ないため。見出し横の「開く」で展開し、チェック状態はこれまで通り端末に保存する
- **費用の表示は入れない**。日別の有料費、上部の費用サマリー、ヒーローの費用バッジをすべて実装したが撤去済み。理由は、施設利用料だけでは実態（食費・交通費が最大）とかけ離れ、かえって誤解を生むため。料金は各スポット行の説明文に書くだけにとどめる
- フッターにキャッチコピーを置かない。写真クレジットは**ヒーロー画像の右下に小さく重ねる**（CC BY/BY-SAは表示が使用条件なので消せない。完全に消したいなら旅行後に自分たちの写真へ差し替えるかCC0素材にする、と説明する）
- **設備やルールのアイコン化は不採用**。焚き火・花火・ペットに斜線を重ねるアイコンセットを試作したが、アイコンは縦にラベルを持つぶん行が高くなり、文字より縦が伸びた（745px→765px、ラベル無しでも709px）。禁止事項の識別性だけは高いので、必要になったらそこだけ検討する
- **行全体をカテゴリ色で塗るのは不採用**。「今日の目玉」の強調が埋もれ、項目表の枠が背景に沈み、1日に6色前後が並んで騒がしくなる。色を足すなら左端に4pxのラインまで
- 比較案（同日程の2案）は両方しおり化してよい：2案目は名前に「（比較案）」、末尾memoに相互リンクと判断基準（何を見ていつ決めるか）を書く。カレンダーは同日程2本の帯を積んで表示できる。決定後に負けた方の<旅名>.htmlとindexの帯を削除する

### 他AIの案と統合するときの進め方
ユーザーが他AI（ChatGPT等）にも同じ計画を依頼し、両案を突き合わせることがある。
1. 依頼文を作るときは、条件・確認してほしい項目・情報源の扱い（公式を一次情報とする／不確実は「要確認」と明記）まで書く。コピーしやすいようコードブロックで渡す
2. 返ってきた案は**歓迎するのではなく検証する**。特に日付・休館日・料金・営業期間は公式で裏取りする
3. **自分の案の誤りが見つかったら即座に認めて、相手の案を骨格に採用する**。実例：祝日の並び（9/22が国民の休日）と美術館の休館日（9/24）を他AI側が正しく指摘し、こちらの案は前提から崩れていた
4. 統合版では「どちらの案から何を採ったか」を明示し、両案で食い違う点（連泊可否など）は未解決として確認事項に残す

### 旧形式ページをテンプレートへ統合する手順
手書き時代のページ（沖縄・白浜など）を共通テンプレートへ寄せるときは、HTMLを書き直さず機械抽出→JSON化→ビルドで行う。
1. 本番HTMLを取得し、`<section class="day">`・`.spot-row`・`var DM`・WX_DAYSを正規表現で抽出
2. スポット行の連番と `DM[n]` のピン配列を順番で突き合わせて座標を復元。旧カテゴリ（海・景／学ぶ等）は固定7種へ写像し、名前に「湯・温泉・風呂」を含むものは温泉へ寄せる
3. 旧ページ固有の良い機能はテンプレート側へ移植して全旅で使えるようにする（沖縄の「食事の候補」ブロック＋オレンジのA/Bピン、宿が動く旅の日別天気地点、フライト行のnote）
4. 変換後は行数・ピン数・食事候補数を旧版と照合し、3幅QAを通してからデプロイ
5. 統合によりヒーローのスポット数が旧表示と変わることがある（重複する宿を1つに数えるなど）。ユーザーに一言添える

### index.html（トップ）の確定仕様
- カレンダーは常に今月始まり（今日ボタン・月送りあり）。旅の最初の月を開く実装だと毎回めくる手間が出るため変更済み。過去旅はカード一覧で「済（非表示）」トグル
- 旅の追加・削除は trips 配列の1行編集＋（削除時）該当htmlのGitHub上の削除

### PWA / Service Worker（sw.js）の鉄則
- HTMLはネットワーク優先・圏外時のみキャッシュ。`r.ok` でない応答（404等）は絶対にキャッシュしない（デプロイ中の閲覧で壊れた表示が固定される事故の再発防止）
- 天気API（open-meteo / jma.go.jp）はキャッシュしない。CDN・地図タイルはキャッシュ優先＋裏で更新
- SWのロジックを変えたらCACHE名の版数を上げる（旧キャッシュが自動一掃される）。ユーザーへの復旧案内は「2回読み込み」→ダメなら「サイト設定→データ削除」
- 新しい旅をオフライン対応したければ sw.js の CORE に <旅名>.html を追加

### LINE共有対策（template組込み済み）
- UAが `Line/` ならURLに `?openExternalBrowser=1` を付けて自動で外部ブラウザへ（LINE公式パラメータ）。地図が出ない報告の定番原因はアプリ内ブラウザ
- 地図サイズ再計算はload後 400/1500/3000ms の3回リトライ（他アプリのWebView対策）

### デプロイ運用（事故防止の要）
- 複数ファイルは必ず tools/deploy.py で1コミットにする。Contents APIで1ファイルずつPUTするとVercelデプロイが多重に走り、その最中の閲覧が404/中途半端になる（→SW事故の引き金になった）
- BUILD_TAGは `YYYY-MM-DD`＋英小文字（同日2回目は b,c…）。`<head>`コメントと`console.log`に入り、デプロイ後に本番HTMLをgrepして確認する
- トークンは会話でその都度もらう fine-grained PAT（対象リポジトリ限定・Contents: Read and write。Git Data APIもこの権限で動く）。記憶に保存しない。無ければ作成手順（github.com/settings/personal-access-tokens/new →リポジトリ限定→Contents RW）を案内し、貼られたら即コミット。手動派には `https://github.com/taka-a1111/trip-plan/upload/main` を案内
- ユーザーから「アップ済み？」と聞かれたら本番URLを実際にgrepして事実で答える

### QA（tools/qa.py＋目視）
- 3幅で横スクロール0・Leaflet地図数・JSエラー0を確認（空の日別地図はJSでdisplay:noneになるガードあり）
- SW登録は https のみ（file://ではエラーになるためガード済み）
- 旅行中日付の挙動はPlaywrightの `add_init_script` でDateを偽装してテストできる
- str_replace/regexパッチは必ずassertで一致確認し、パッチ後にQAを通してからデプロイ（空振りパッチを無検証でコミットしない）。改行・空白・装飾記号の有無でパターンが一致しないことがあるので、assertが落ちたら実ファイルから該当箇所をgrepで再取得してから当て直す。パッチが途中で落ちた回のファイルをそのままコミットしない
- 内容を差し替えたら「差し替えた場所」だけでなく「その素材を共有していた場所」も確認する。1か所を直したつもりで別の場所が壊れる事故は、素材の使い回しから起きる
- 機能を追加したらその機能自体を検証する（例：Playwrightでポップアップを実際にクリックして値が入るか、`add_init_script` でDateを偽装して旅行中の挙動を見る）。ビルドが通っただけで「できた」と報告しない

### 旅行終了後のアーカイブ手順
1. index側は日付経過で自動的に「済」表示になる
2. ヒーロー・日別写真を家族の実写真へ差し替え（権利クリア）、実費をmemoに記録
3. 過去の日付の天気チップは自動的に出なくなる（正常）
4. 旅行中に便利だった「当日だけ表示」の日付タブがある場合、初期表示を全日に戻す（終了後に1日目しか出ずスクロールしても続きが見えない、という指摘あり）
5. 不要になった比較案・旧プランのファイルはGitHub上から削除し、indexの帯も消す

---

## チェックリスト

### 段階1（テキスト計画）
- [ ] `date` で全日の曜日を確認したか（変更のたびに再実行）
- [ ] 宿・キャンプ場を最新の優先条件で裏取りしたか（営業・臨時閉鎖・予約要否・クマ・増水）
- [ ] 候補地とユーザーが挙げた行きたい場所について、直近1年のクマ目撃を調べたか。該当があれば伝えて外したか
- [ ] 道路の注意を調べたか（ナビ誤案内・キャンプ場までの悪路・高速の工事規制・連休の渋滞・給油・冬季閉鎖）
- [ ] 各スポットを曜日×定休日で照合したか（8月の事前予約制・夏休みダイヤ等の例外も）
- [ ] 日程変更時、既存予約の整合とキャンセル要否を確認したか（Gmail送信者検索で裏取り）
- [ ] 2案比較なら比較表＋体感気温比較を出したか
- [ ] 規定フォーマットで出力し、しおり化を一度だけ提案したか

### 段階2（しおり）
- [ ] trips/<旅名>.json＋build.py で生成したか（HTML直編集をしていないか）
- [ ] 共通の修正は template.html に入れ、全旅を再ビルドしたか
- [ ] 曜日バッジ・料金・予約制の最新情報を反映したか（クマ・増水noticeを入れたか）
- [ ] 追加した全スポットに公式URLを入れたか（調べずに空欄にしていないか）
- [ ] 各スポットに一言＋項目表を入れたか。項目とピル・noticeで同じことを二度書いていないか
- [ ] 食事を含む項目は実店舗名＋地図リンクにしたか（その日の曜日に営業しているか確認したか）
- [ ] 写真は権利クリアか。クレジット表記・「イメージ」明記をしたか
- [ ] 日別写真は1日1ファイルか（重複・欠損を機械点検したか）。外したスポットの写真が残っていないか
- [ ] qa.py 3幅OK＋天気チップが日付と同一行か
- [ ] index.html の trips 配列を更新したか（新規/削除）
- [ ] deploy.py で1コミットにしたか。BUILD_TAGを上げ、本番grepで確認したか
- [ ] SWを変えたならCACHE版数を上げたか。エラー応答をキャッシュしない実装のままか
- [ ] ユーザーへの案内（ハードリフレッシュ・BUILD_TAG・LINEはそのまま共有可）を添えたか
- [ ] 修正後、本番URLを返答に添えたか

---

## 未解決の課題

- **過去の旅の実績気温表示**：終了した旅は予報APIの対象外になるため天気が出ない。Open-Meteoのアーカイブ（`archive-api.open-meteo.com`）で実績値を取得してJSONに焼き込み、静的チップとして出す実装を試みたが、描画まで到達できていない（予報APIが過去日でエラーを返し `render()` が走らない経路が残っている疑い）。再着手するなら、fetch失敗時にも `draw()` を必ず呼ぶところから確認する

---

## このスキルファイルの版管理

- プロジェクトへ渡す配布版はファイル名にバージョンを付ける（`SKILL_ver2.1.md`）。プロジェクト内は同名で上書きされず並ぶため、名前で最新が分かるようにする
- リポジトリ側は `SKILL.md` 固定。本文冒頭の `version:` / `last updated:` と末尾の更新履歴で版を管理する（最新はリポジトリ版が正）
- 番号の付け方：文言修正・小さな追記＝マイナー（2.0→2.1）、構成変更・仕組みの刷新＝メジャー（1.0→2.0）
- 更新したら必ず更新履歴に1行追記してからコミットする

---

## 更新履歴

- ver2.68（2026-08-29）伊豆の初日を三津シーパラダイスから三島スカイウォークへ差し替え、最終日の昼食を沼津港に、雨の候補を沼津港深海水族館に変更。
- ver2.67（2026-08-29）カードの表に家族4人ぶんの料金を1行追加（全7旅・高さは固定でそろえた）。
- ver2.66（2026-08-29）伊豆高原（2026/9/19–24・伊豆海の森スマイルキャンプ場拠点）を北陸案の比較案として新規作成。北陸のmemoに福井の大雨被害と相互リンクを追記。
- ver2.65（2026-08-29）日別地図の表示範囲に食事・買い出し・雨天の全候補を含めるようにした（座標0の行は除外）。琵琶湖の2泊目・3泊目に希望が丘 野外活動センターのキャンプを追加し、蒲生野の湯を2日目の温泉に。
- ver2.64（2026-08-29）ホテル泊の日は日帰り温泉を入れないルールを追加し、琵琶湖の初日から極楽湯彦根を削除。
- ver2.63（2026-08-29）琵琶湖の初日をアパホテル彦根南（和室・予約済み）に差し替え、荒神山公園と彦根城の彦根拠点に組み替え。2泊目・3泊目の宿は未定としてnoticeとmemoに明記。
- ver2.62（2026-08-29）キャンプ場のカードに「なっぷ」ボタンを追加（`points[].nap`）。北陸のなっぷリンクがトップページURLだったのを施設ページへ修正し、白浜グランパスも予約からなっぷへ移動。マイアミ浜のIN/OUTを公式に合わせて修正。
- ver2.61（2026-08-29）琵琶湖・湖南（2026/10/9–12・マイアミ浜拠点）を新規作成。津波浸水域とクマ目撃を理由に浜名湖・琵琶湖湖西を外した検討経緯を反映し、湖南の平野部＋湖岸で組んだ。
- ver2.60（2026-08-29）北陸9/23のいしかわ動物園をサイエンスヒルズこまつ ひととものづくり科学館に差し替え（目玉は昆虫館へ、雨の候補からサイエンスヒルズを外して恐竜博物館を追加）。スポットを入れ替えたら `rt_order`・`visit_order`・`pins`・その日の `rain` の重複まで見直すことを再確認。
- ver2.59（2026-08-29）スポットカードの開閉をカード全体のタップで行えるようにした（地図・公式・予約のボタン上を除く）。
- ver2.58（2026-08-28）食事・雨天ブロックの見出しから、開いたときの説明文「チェックすると地図にピンが出ます」を削除（閉じているときの「タップで開く」だけ残す）。
- ver2.57（2026-08-28）雨の日の候補を1日3件から5件（A〜E）に増やした。
- ver2.56（2026-08-28）雨の日の候補を各日の食事ブロックの下へ移設（1日3件・折りたたみ・チェックでA/B/Cのピン・開いているあいだ地図が追従）。ページ末尾の雨セクションと日付バーの雨チップを削除。服装の目安を昼／夜／寝の3語に短縮。
- ver2.55（2026-08-28）地図・公式・予約のボタンをカードの表（常時表示）へ移動。雨の日の候補も1件ずつの折りたたみ式にして既定を閉じ、「すべて開く」と地図ボタンを追加。雨の地図の追従を解除。
- ver2.54（2026-08-28）閉じたスポットカードの高さを名前2行ぶんに統一（全旅・380/430/1000pxで1種類）。目玉行の名前拡大を廃止し、白浜の説明的なスポット名3件を施設名に直した。
- ver2.53（2026-08-28）「今日の目玉」を文字バッジから星マークへ変更。横幅が空いたので全スポット行で時刻・番号・名前・タグが1行に並び、縦位置が完全にそろった。
- ver2.52（2026-08-28）見出し行の縦位置を中央にそろえた（矢印を絶対配置に、バッジを `.s-meta` でまとめ、目玉なしの行は折り返し禁止、名前は語の切れ目で折り返す）。
- ver2.51（2026-08-28）日別地図の追従を、食事の候補を開いているあいだだけに限定（既定は追従しない）。
- ver2.50（2026-08-28）スポット名を施設名だけに統一し、用途・地名・役割の（かっこ書き）を全旅から削除。サイト区分などの予約情報は項目表へ移した。
- ver2.49（2026-08-28）移動行のnoteを廃止し、区間・距離・所要だけの表示に統一（フライト等は `keep_note` で例外）。ヘッダーを「戻る／旅名＋日付／共有／メニュー」に刷新し、ボタンの枠と影を外して高さを62→57pxに。ナビはメニューへ移動。
- ver2.48（2026-08-28）食事と買い出しの候補ブロックを折りたたみ式に（既定は閉じ）。ページ全長は17,885→13,851px。
- ver2.47（2026-08-28）移動行の装飾を外して高さを圧縮（背景・バッジ・大きい点を廃止）。ページ全長は18,271→17,900px台。
- ver2.46（2026-08-28）移動行の表記を「距離＋所要」だけに簡潔化し、その日の最初の移動にのみ出発地を明記。
- ver2.45（2026-08-28）スポットを1件ずつ折りたためるようにした（既定は閉じて名前だけ表示。各日に「すべて開く」）。ページ全長は約23,000→19,300px。
- ver2.44（2026-08-28）雨の日の候補を料金・営業・定休つきのスポット行と同じ書式に統一し、地図を上部固定に。
- ver2.43（2026-08-28）日付バーに「雨天」チップを追加。天気帯の地名を他の文字サイズに統一。雨の日ブロックの余白を詰め、地図をstaticにすると横スクロールが出る事故を記録。
- ver2.42（2026-08-28）末尾メモを重複排除で厳選し、雨の日の候補（室内施設10か所＋地図ピン）をページ末尾に追加。
- ver2.41（2026-08-28）宿の受付を🕐🛎️🚪の3行に簡潔化し、電話番号をtel:リンクに。キャンプ場の料金を👪の1行にまとめた。
- ver2.40（2026-08-27）料金を👨‍🦰大人／🧒子ども／👪家族4人の3行に絵文字化し、年齢・学年は括弧内へ。🅿️はある場合は金額のみ、ない場合だけ✕に統一。
- ver2.39（2026-08-27）駐車場を独立項目から料金欄の🅿️行へ移し、ゴミのルールを「回収か持ち帰りか」だけに簡潔化。
- ver2.38（2026-08-27）備品・ルールの小項目を1行1項目に戻し、短いものだけ2つ並べる方針に。補足文は必ず改行して下に置く。
- ver2.37（2026-08-27）項目表をラベル行頭の段落型に変更（キャンプ場カード881→756px、温泉633→616px）。値がフレックス縦積みで3行に割れるバグを修正し、絵文字入り項目はNBSPで連結。
- ver2.36（2026-08-27）備品の並びをせっけん→シャンプー→ドライヤーに変更し、駐車場に🅿️、キャンプ場のルールに🚯🔥🎆🐕🔇の絵文字を導入。矛盾する補足文を削除。
- ver2.35（2026-08-27）温泉の備品を絵文字＋◯✕△の1行表示に変更（🧴シャンプー・🧼ボディソープ・🌬️ドライヤー）。
- ver2.34（2026-08-27）温泉の項目に「備品」（石けん類・シャンプー・ドライヤーの有無と有料無料）を必須で入れるルールを追加。
- ver2.33（2026-08-27）持ち物リストを整理（重複の削除、トイレ・ごみの枠を新設、ヘッドライトと食料用密閉コンテナを追加、ドライヤーとパスポートを削除）。
- ver2.32（2026-08-27）施設間の移動行を全区間に入れ、距離と所要をOSRMで実測して記載。地図のピンをスポット行と同じカテゴリ色に統一。
- ver2.31（2026-08-27）ヘッダーにナビ・共有のアイコンを追加し、ハンバーガーメニューを日程リンクから「全体地図・天気・持ち物・予約と確認事項」に入れ替えた。
- ver2.30（2026-08-27）チェック時に地図を寄せる動作を削除（位置関係が見えなくなるため）。ピンを最前面に出す処理だけ残した。
- ver2.29（2026-08-27）食事の表の1列目の見出しを「昼の候補／夜の候補／スーパーの候補」に変え、日別地図を表の前へ移してスマホでは上部に固定表示するようにした。
- ver2.28（2026-08-27）温泉・観光・遊ぶの項目を「料金／営業／定休／駐車場」に統一し、駐車場は無料・有料と料金を書くルールを追加。日別地図の余白を詰め（pad 0.12・maxZoom 15）、食事ピンを最前面に出して隠れないようにした。
- ver2.27（2026-08-27）スポット行を「見出し行＋全幅本文」に刷新してカード化（左端4pxのカテゴリ色つき）し、予定時刻と番号をスポット名に対して縦センタリング。項目表（`rows[].fields`）とカテゴリ別の標準セットを新設し、ラベルは右揃え＋点線区切り、一言の解説は残しつつ営業時間ピル等との重複を禁止。食事の候補を「チェック／店舗名／口コミ／予算（1人あたり）／営業時間／定休日」の横スクロールできる表に変え、昼・夜それぞれ6軒（キャンプ旅は買い出し枠も）・口コミ300件以上（埋まらない枠はファミレスで補完）・動線（昼はその時間にいるスポットの近く、夜は最後のスポットから宿への帰り道）・ジャンルを散らす・地元の名店とご当地グルメを優先・ご当地グルメと有名店は2日以上に載せる、を明文化。チェックした店は色付けし、同じチェーンの別店舗を含めて他の日を薄くし、その日の地図にピンを立てる。日ごとに「この日の動きを地図で開く」ボタン（前日の宿泊地→スポット→選んだ店→その日の宿泊地）を追加。画面下の日付バーを採用に方針変更（曜日の色分け、祝日の自動判定、当日ハイライト）し、旅行期間中の当日自動スクロールを追加。予報が出ていない期間は過去5年平均の気温（平年）を天気チップの位置に出す。項目表は料金・営業・定休だけに絞り（泊まるは受付・料金・予約・ルール）、所要・移動・昼食と予約番号を廃止して、眺め・見どころ・注意などは説明文へ移した。ラベルは薄いチップに載せて縦横センタリング。地図のピンは昼・夜・買い出しで色と字を変える。文字サイズと文字色の下限を明記。日ごとのアコーディオン・アイコン化・行全体の着色は検討のうえ不採用と記録。
- ver2.7（2026-08-26）予定時刻バッジ（rows[].plan）を追加し、出発は9時以降・キャンプ場の受付終了時刻から逆算する設計ルールを新設。noticeは2文以内にして詳細はnoteへ移す、その日の主役を1つ強調する、スポット数は観光スポットのみ数える、日帰り温泉は全日に1か所ずつ入れる、を明記。シャワー室にコンセントが無い前提でドライヤーは温泉施設を使う運用を追加。
- ver2.6（2026-08-22）公式URLはスポット追加のたびに必ず調べて入れるルールを明記（探す順序、見つからないときは会話で伝える、裏取り中に判明した料金・休館日・利用制限もnoteへ反映）。修正・デプロイ後は本番URLを必ず添える運用を追加。
- ver2.5（2026-08-22）道路とルートの注意を必ず調べる節を新設（ナビの誤案内、キャンプ場までの最後の数kmの路面をレビューで確認、高速のリニューアル工事、連休の渋滞ピーク、山間部の給油、雨天時の冠水・落石、冬季閉鎖）。クマ情報と同じ重みで毎回確認し、移動行のnoteと.noticeに書く運用を明記。
- ver2.4（2026-08-22）直近1年以内にクマの目撃・被害があったキャンプ場および周辺は候補から外すルールを必須化。ユーザーが挙げた行きたい場所についても同様に確認し、該当すれば時期・場所・距離を添えて知らせる運用を追加。秋は評価が変わる点、立ち寄り先の山間観光地も対象に含める点を明記。
- ver2.3（2026-08-19）日別写真は1日1ファイルとし、同名ファイルを複数の日で共有しないルールを追加（共有していると差し替え時に無関係な日の挿絵が入れ替わる）。写真の重複・欠損を機械点検する手順と、素材を共有していた箇所も確認する原則、日程から外したスポットの写真を見直すルールを追記。
- ver2.2（2026-08-19）天気判定を気象庁公式予報ベースの総合判定に変更（細分区の指定必須、雷は複数モデル合意か予報文の「雷」、モデル1本では出さない）。アイコンの識別性、チップの表記（%のみ・大きめ）、予報が出るのは直近16日までである旨を追記。祝日の並び（国民の休日）と月曜休館の振替を必ず確認するルールを新設。1日の運転時間と拠点連泊を設計指標に、同行者に合わせたスポット選び（子ども優先・神社仏閣は控えめ）を追加。フローティングボタンと費用表示がいずれも却下された経緯、フッターのキャッチコピー削除と写真クレジットのヒーロー内表示を明記。他AIの案と統合する手順、未解決の課題を追加。
- ver2.1（2026-08-18）情報源の優先順位（公式＝一次情報／まとめサイトの要約を鵜呑みにしない）を新設。天気ポップアップに他社数値を並べない方針、天気は都度取得である旨、旅行後は日付タブを全日表示へ戻す運用、旧形式ページのテンプレート統合手順、機能追加時は機能自体をQAする原則、パッチ空振り時の対処、スキルファイル自身の版管理ルールを追加。
- ver2.0（2026-08-17）大幅改訂。しおり制作を trip-plan リポジトリのビルドシステム（template.html＋trips/JSON＋build/qa/deploy）に統一。全4旅のフォーマット統一、食事の候補ブロック・日別天気地点・日別写真帯・天気チップ小型化・LINE共有対策・PWA/Service Workerの鉄則・1コミットデプロイ・BUILD_TAG運用・クマ/増水の裏取りルール・比較案2本立て運用・アーカイブ手順を追加。フローティングボタン禁止を明記。
- ver1.0（〜2026-08-16）テキスト計画ワークフローと手書きHTMLしおりの確定デザイン版。
